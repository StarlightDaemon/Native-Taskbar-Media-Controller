# Workspace Audit — native-taskbar-media-controller

| Field | Value |
|---|---|
| Repository | `E:\Citadel/native-taskbar-media-controller` |
| Commit | `c1fb551` (HEAD, branch `main`) |
| Branch | `main` |
| Audit date | 2026-05-25 |
| Protocol | `.raiden/writ/WORKSPACE_AUDIT_PROTOCOL.md` (RAIDEN Edict 0.5.0) |
| Auditor | Claude Sonnet 4.6 |

---

## Executive Summary

| Severity | Count |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 3 |
| Low | 2 |
| Info | 3 |

**Total actionable findings: 5** (Medium 3, Low 2); 3 Info findings carry no required action.

| Remediation class | Count |
|---|---|
| MECHANICAL | 1 |
| TARGETED-FIX | 0 |
| DOC-EDIT | 4 |
| SPECULATIVE-TRIAGE | 0 |
| SECURITY-CRITICAL | 0 |
| CONFIG/CI | 0 |
| MULTI-FILE-REFACTOR | 0 |

**Top 3 priorities:**

1. **(Medium / DOC-EDIT)** In-mod `==WindhawkModReadme==` (the user-facing Windhawk page description) is missing hover flyout, middle-click to close, text crossfade, widget fade, and smooth progress interpolation — all shipped in v1.3.0–v1.4.4. This is not addressed by the current working tree changes and requires new work.
2. **(Medium / DOC-EDIT)** `README.md` and `CURRENT_STATE.md` are 4–6 versions stale at HEAD (v1.1.0 and v1.3.0 respectively). Working tree contains corrections but they are uncommitted.
3. **(Info / MECHANICAL)** Working tree `last-audit.md` stores a conversation ID instead of a file path for the report reference — will break the audit trail if committed as-is. Requires one-line fix before committing.

---

## Orientation Layer

- **Canonical docs:** `AGENTS.md` → `.raiden/README.md` → `.raiden/state/CURRENT_STATE.md` → `.raiden/state/OPEN_LOOPS.md`. All four present and readable.
- **Routing config:** `.raiden/routing.md`, `.raiden/models.md`, `.raiden/agents.md`, `RAIDEN.md`, `routing.md`, `models.md` — none found anywhere. **Class defaults applied for all findings.**
- **Open loops:** All open loops are closed as of HEAD. `OPEN_LOOPS.md` contains only `~~struck-through~~` closed entries — no active open loops.
- **RAIDEN edict version:** 0.5.0 (confirmed in `.raiden/instance/metadata.json`).
- **Layout notes:** Single-source C++ Windhawk mod (`native-taskbar-media-controller.wh.cpp`, ~2498 lines at HEAD). Supporting materials: `forks/` (11 reference copies), `fork-reports/`, `prompts/`, `.raiden/` control plane. No build system, no package manager, no CI configuration. Working tree has 6 uncommitted modified files from a previous session — see Appendix.

---

## Findings

### Category 1 — Exposed Secrets

`gitleaks` and `trufflehog` are not installed. Manual pattern-search performed across the full working tree (`git ls-files`) and complete `git log -p --all` output for: AWS AKIA/ASIA keys, GCP service-account JSON, Azure connection strings, Stripe `sk_live_*`, GitHub `ghp_*`/`github_pat_*`, Slack `xox*`, OpenAI `sk-*`, Anthropic `sk-ant-*`, committed credential files (`.env*`, `*.pem`, `id_rsa`, `*.keystore`, `credentials.json`, `service-account*.json`), and long base64/hex token patterns.

No pattern matches found in any diff content or tracked files. `WORK_LOG.md` contains prose references to PATs used and revoked within single sessions; targeted `git log -p` grep for `ghp_` and `github_pat_` produced zero diff hits across all commits.

`.gitignore` (added since prior audit) covers `.env*`, `*.pem`, `*.key`, `*.keystore`, and all standard credential-file patterns; secret-bearing paths are excluded.

**No findings.**

---

### Category 2 — Dependency Vulnerabilities

**Production dependencies: 0 findings. Development dependencies: 0 findings.**

No package manager; no dependency manifest. The sole source file links against Windows SDK and WinRT APIs provided by Windhawk's mod runtime and the host OS. All dependency audit tools (`npm audit`, `pip-audit`, `cargo audit`, `govulncheck`, `bundle audit`) are inapplicable — see Appendix.

---

### Category 3 — Outdated Dependencies & Deprecated APIs

No package manager dependencies to assess. The mod targets `Windows.Media.Control` and `Windows.UI.Xaml.*` as provided by Windows 11 (22H2+). Both are current and actively supported. No deprecated API usage detected in the primary source.

**`Windows.UI.Xaml`** (as distinct from WinUI 3's `Microsoft.UI.Xaml`) is intentional and correct for Windhawk taskbar injection; the Windows 11 taskbar itself uses `Windows.UI.Xaml`. No deprecation concern in this context.

**No findings.**

---

### Category 4 — Documentation Drift

#### 4a. README setup/run command resolution

No `npm run`, `cargo build`, `make`, or equivalent build commands referenced. Install flow (load `.wh.cpp` via Windhawk → Develop → Load mod from file) is correct; the file exists at the documented path.

**No findings.**

#### 4b. External link check

Four external links appear in canonical docs. All checked via HTTP GET with 10-second timeout:

| URL | Location | Status |
|---|---|---|
| `https://windhawk.net` | README.md | 200 OK |
| `https://github.com/bbmaster123/FWFU` | README.md | 200 OK |
| `https://github.com/ramensoftware/windhawk-mods/blob/53d96781b3215f0a082908a2539cafe178e8895a/mods/taskbar-music-lounge.wh.cpp` | README.md | 200 OK |
| `https://github.com/StarlightDaemon/Native-Taskbar-Media-Controller` | CURRENT_STATE.md | 200 OK |

Previously broken link (`github.com/Hashah2311/taskbar-music-lounge`, 404 in the 2026-05-21 audit) has been replaced with the preserved source link in the Windhawk community mods repo, with an inline note that the author's account no longer exists. Positive signal.

**No findings.**

#### 4c. Stale TODO/FIXME markers

`grep` across all tracked files outside `forks/` for `TODO`, `FIXME`, `HACK`, `XXX`: markers found only in `.raiden/writ/` (describing the audit protocol's own requirements) and one historical archive doc — none in primary source, state files, or prompt files. No markers in `native-taskbar-media-controller.wh.cpp`.

**No findings.**

#### 4d. Badge mismatches and version drift

**Finding DOC-1 (Medium — actionable):** `CURRENT_STATE.md` at HEAD declares **v1.3.0** as the current version. The HEAD commit `c1fb551` is tagged work at v1.4.7 plus two additional fix commits (`2f8adcd`, `c1fb551`) that are untagged. The file is missing: v1.4.4 hover flyout (SC-FLY-1), v1.4.5–v1.4.7 flyout polish (sizing, system theme colors, font reduction), Chameleon/BlurredArt removal, and v1.4.x UX features. An operator or agent reading `CURRENT_STATE.md` gets a picture that is 4 feature versions stale and omits the removal of significant features (Chameleon mode).

| Field | Value |
|---|---|
| ID | DOC-1 |
| Severity | Medium |
| Class | DOC-EDIT |
| Recommended model | Gemini 3 Flash or Haiku 4.5 |
| Location | `.raiden/state/CURRENT_STATE.md` (HEAD) |
| Evidence | File header: "v1.3.0 — Committed (2026-05-24)"; HEAD commit is v1.4.7+2 |
| Action | Update `CURRENT_STATE.md` to reflect v1.4.4 flyout, v1.4.5–v1.4.7 polish, Chameleon removal, and the v1.4.8 audit-hardening changes queued in the working tree. **Note:** a corrected version exists in the working tree and is ready to commit. |
| Seed | In `.raiden/state/CURRENT_STATE.md`, update the version header and feature sections to cover v1.4.4 (SC-FLY-1 flyout panel), v1.4.5–v1.4.7 (flyout sizing, system theme colors, font reduction), Chameleon/BlurredArt removal, and v1.4.8 (atomic g_FlyoutHwnd, UnregisterClassW, RemoveWidget threading fix). The working tree already contains this correction — commit it. |

**Finding DOC-2 (Medium — actionable):** `README.md` at HEAD declares **"Status: v1.1.0"**. The mod is at v1.4.7+2 commits. The features list is 6 versions behind: missing hover flyout, middle-click to close, text crossfade, widget fade in/out, smooth progress interpolation, and `AcrylicBrush`-only background. The settings table still references the "Chameleon" theme option which was removed.

| Field | Value |
|---|---|
| ID | DOC-2 |
| Severity | Medium |
| Class | DOC-EDIT |
| Recommended model | Gemini 3 Flash or Haiku 4.5 |
| Location | `README.md` line 5 (HEAD) |
| Evidence | `README.md`: "Status: v1.1.0"; `@version` in source: 1.4.7 |
| Action | Update status version, add flyout and middle-click to the feature list, remove Chameleon from the settings table, add Acrylic-only background description. **Note:** a corrected version exists in the working tree and is ready to commit. |
| Seed | In `README.md`, update "Status: v1.1.0" to current version, add hover flyout and middle-click features to the bullet list, update the settings table to remove Chameleon and add flyout transparency. Working tree already contains this correction — commit it. |

**Finding DOC-3 (Medium — actionable):** The `==WindhawkModReadme==` embedded section (lines 12–58 of `native-taskbar-media-controller.wh.cpp`) is the description users see on the Windhawk mod page. At HEAD it lists 9 features but is missing all of: hover flyout panel, middle-click to close, text crossfade on track changes, widget fade in/out, and smooth progress interpolation — all shipped in v1.3.0–v1.4.4. This is the primary user-facing marketing copy for the mod; an incomplete feature list undersells the mod's capabilities and may confuse users comparing it to forks. This finding is **not addressed by the current working tree changes** and requires new editing work.

| Field | Value |
|---|---|
| ID | DOC-3 |
| Severity | Medium |
| Class | DOC-EDIT |
| Recommended model | Gemini 3 Flash or Haiku 4.5 |
| Location | `native-taskbar-media-controller.wh.cpp` lines 12–58 (`==WindhawkModReadme==`) |
| Evidence | In-mod readme lists: now playing, playback controls, position timestamp, multi-session, audiobook mode, fullscreen auto-hide, adaptive text color, double-click to focus, track progress bar. Missing: hover flyout, middle-click, crossfade, widget fade, smooth progress. |
| Action | Add the missing shipped features to the `==WindhawkModReadme==` feature list. Consider grouping as UX polish bullets. Keep descriptions brief (mod page has no word limit but users scan). Do not add features not yet shipped (SC-HT-1, SC-GR-1). |
| Seed | In `native-taskbar-media-controller.wh.cpp` inside the `==WindhawkModReadme==` block (lines ~21–41), add bullet entries for: hover flyout (v1.4.4 — hover the widget to see an expanded panel with album art and track info), middle-click to close (v1.3.0 — stops the active media session), text crossfade (v1.2.0 — smooth opacity fade on track changes), widget fade in/out (v1.2.0), and smooth progress interpolation (v1.2.0 — 500ms timer fills bar between SMTC ticks). |

**Finding DOC-4 (Low — actionable):** `GOALS.md` at HEAD claims the latest stable tag is `v1.1.0` ("Post-release: v1.0.x–v1.1.0 | Complete | main — latest tag v1.1.0"). SC-FLY-1 (flyout), SC-UI-1 (blurred art), and SC-M-3 (middle-click) are all listed under "not yet implemented," though all three shipped (SC-FLY-1 in v1.4.4, SC-UI-1 shipped then removed, SC-M-3 in v1.3.0). SC-HT-2 is marked as "used for Chameleon" but Chameleon was removed.

| Field | Value |
|---|---|
| ID | DOC-4 |
| Severity | Low |
| Class | DOC-EDIT |
| Recommended model | Gemini 3 Flash or Haiku 4.5 |
| Location | `.raiden/state/GOALS.md` (HEAD) |
| Evidence | "latest tag v1.1.0"; SC-FLY-1/SC-UI-1/SC-M-3 listed as unimplemented |
| Action | Update phase status row to v1.4.8, mark SC-FLY-1/SC-UI-1/SC-M-3 as shipped (✓), note SC-UI-1 shipped and subsequently removed. **Note:** a corrected version exists in the working tree and is ready to commit. |
| Seed | In `.raiden/state/GOALS.md`, update "Post-release" phase row to v1.4.8, move SC-FLY-1 and SC-M-3 to the shipped list (✓), mark SC-UI-1 as "✓ shipped v1.3.0 → removed v1.4.7 (theme simplification)", and note SC-HT-2 (color quantization) was used for Chameleon and removed alongside it. Working tree already contains this correction — commit it. |

---

### Category 5 — Repository Hygiene

`.gitignore` is present and covers: C++ build outputs (`*.obj`, `*.exe`, `*.dll`, `*.pdb`, `*.lib`, etc.), Visual Studio and VS Code directories, Windows OS junk (`Thumbs.db`, `desktop.ini`), editor temporaries, runtime diagnostics, and `check_xaml.cpp`. No accidentally committed build artifacts, large binaries (all tracked files < 1 MB), IDE config, or OS junk detected.

**Finding HYG-1 (Low — actionable):** HEAD (`c1fb551`) is 2 commits ahead of the `v1.4.7` git tag, and `@version` in the source file still reads `1.4.7`. The two untagged commits — `2f8adcd` (fix: reduce flyout font sizes and use end-ellipsis) and `c1fb551` (fix: system theme integration) — both represent shipped, user-installable improvements to the hover flyout. A user installing at HEAD gets a different (improved) mod than what `v1.4.7` describes, but the version number does not reflect this. Working tree bumps `@version` to `1.4.8` but this change is uncommitted and untagged.

| Field | Value |
|---|---|
| ID | HYG-1 |
| Severity | Low |
| Class | MECHANICAL |
| Recommended model | Gemini 3 Flash or Haiku 4.5 |
| Location | `native-taskbar-media-controller.wh.cpp` line 5; `git tag` |
| Evidence | `git log --oneline v1.4.7..HEAD` → 2 commits; `@version 1.4.7` in source at HEAD |
| Action | Commit the working tree `@version 1.4.8` bump (already written) and tag the resulting commit `v1.4.8`. Push commit and tag to remote. |
| Seed | The working tree already bumps `@version` from `1.4.7` to `1.4.8` in `native-taskbar-media-controller.wh.cpp`. Commit all six working tree modifications, then `git tag v1.4.8 HEAD` and push both the commit and the tag to origin. |

---

### Category 6 — Configuration Risk

No server-side config, no network-facing service, no TLS, no CORS, no database. Mod settings are declared in the Windhawk `==WindhawkModSettings==` block and read via `Wh_GetIntSetting()`. All settings have safe defaults and bounds-checked after load. `Wh_Log()` output goes to Windhawk's own diagnostic viewer, not a public surface.

One minor observation: `g_Settings.offsetX` is clamped to `8` when negative, which matches the documented default of `8` — consistent, no concern. (A prior audit noted this as an inconsistency against a default of `200`; that was stale — the default was `200` in an older version and has since been updated to `8` in both code and docs.)

**No findings.**

---

### Category 7 — License & Legal

`LICENSE` file is present at the repository root: MIT License, copyright 2026 StarlightDaemon. `README.md` declares "MIT — see [LICENSE](LICENSE)." Consistent.

No external dependencies requiring attribution. `forks/` directory contains reference source files for research; each file carries its own Windhawk header attributing the original author. `README.md` Acknowledgements section names all fork authors with live links (all checked 200 OK in this audit).

**No findings.**

---

### Category 8 — Open Observations (Speculative Pass)

**OBS-1 (Info — no action required):** The working tree contains 6 uncommitted modified files: `.gitignore`, `CURRENT_STATE.md`, `GOALS.md`, `last-audit.md`, `README.md`, `native-taskbar-media-controller.wh.cpp`. These are the output of a previous in-session audit pass (2026-05-25) that ran code improvements (atomic `g_FlyoutHwnd`, `UnregisterClassW`, `RemoveWidget` threading fix, version bump to 1.4.8) and doc refreshes. The code and doc changes are correct and address DOC-1, DOC-2, DOC-4, and HYG-1. The session did not commit. These changes should be committed, with the caveat in OBS-2 addressed first.

**OBS-2 (Info — low-action):** The working tree `last-audit.md` stores a conversation UUID (`Antigravity conversation 4758d350-cd69-4d3e-948c-b545d93f2e82`) as the report path instead of a file path. At HEAD, `last-audit.md` correctly points to `audit-reports/audit-2026-05-21-c7a2675.md`. If the working tree version is committed as-is, the audit trail link will be broken. This report (`audit-reports/audit-2026-05-25-c1fb551.md`) is the canonical reference for the current session and should replace the malformed path before committing.

**OBS-3 (Info — no action required):** The `==WindhawkModSettings==` block names the `OffsetX` setting "Gap between widget and system tray (px)" while the external `README.md` settings table calls it "Gap from tray (px)". The labels are functionally synonymous but textually inconsistent. A user comparing the Windhawk UI label with the README would not be confused, but the inconsistency is cosmetically imperfect. Lowest-priority cosmetic fix.

---

## Remediation Plan

Actionable findings only, grouped by class, ordered by severity then ID within class.

### DOC-EDIT

| ID | Severity | Finding | Status | Recommended model |
|---|---|---|---|---|
| DOC-3 | Medium | Add hover flyout, middle-click, crossfade, widget fade, smooth progress to `==WindhawkModReadme==` | Needs new work | Gemini 3 Flash or Haiku 4.5 |
| DOC-1 | Medium | Update `CURRENT_STATE.md` to v1.4.8 | Working tree ready — commit | Gemini 3 Flash or Haiku 4.5 |
| DOC-2 | Medium | Update `README.md` status and feature list | Working tree ready — commit | Gemini 3 Flash or Haiku 4.5 |
| DOC-4 | Low | Update `GOALS.md` phase status and shipped list | Working tree ready — commit | Gemini 3 Flash or Haiku 4.5 |

### MECHANICAL

| ID | Severity | Finding | Status | Recommended model |
|---|---|---|---|---|
| HYG-1 | Low | Commit v1.4.8 version bump; tag and push `v1.4.8` | Working tree ready — commit + tag | Gemini 3 Flash or Haiku 4.5 |

---

## Appendix

### Ecosystems detected

| Ecosystem | Present | Audit tool |
|---|---|---|
| C++ (Win32/WinRT) | Yes | Manual review only — no C++ CVE scanner applicable |
| Node / npm | No | — |
| Python / pip | No | — |
| Rust / Cargo | No | — |
| Go | No | — |
| Ruby / Bundler | No | — |
| PHP / Composer | No | — |
| Java / JVM | No | — |
| .NET | No | — |

### Tools used

- `git log`, `git ls-files`, `git branch`, `git show`, `git tag`, `git log --oneline <tag>..HEAD`
- `grep`, `find` (manual pattern search for secrets, markers, settings)
- `curl` (HTTP GET for all external links)
- Manual source review of `native-taskbar-media-controller.wh.cpp`

### Routing source

Not found — class defaults applied for all findings.

### Hard Skips (tool/ecosystem unavailable)

| Skip | Reason |
|---|---|
| `gitleaks detect --no-git --redact` | `gitleaks` not installed |
| `trufflehog filesystem . --no-update` | `trufflehog` not installed |
| `npm audit` | No `package.json` |
| `cargo audit` | No `Cargo.toml` |
| `pip-audit` | No `requirements.txt` / `pyproject.toml` |
| `govulncheck` | No `go.mod` |
| `bundle audit` | No `Gemfile` |
| `composer audit` | No `composer.json` |

Secrets scanner absence compensated by manual `git log -p` pattern-search across all commits and full working tree grep. No patterns matched.

### Discretionary Skips

None. All mandatory checks were executed or their absence affirmatively documented.

### Working-Tree Context

**6 uncommitted modified files at HEAD** (environment state; not audit findings):

| File | Nature of change |
|---|---|
| `native-taskbar-media-controller.wh.cpp` | Version bump 1.4.7→1.4.8; `g_FlyoutHwnd` → `std::atomic<HWND>`; `UnregisterClassW` after flyout loop; `RemoveWidget` threading fix |
| `.raiden/state/CURRENT_STATE.md` | Rewritten to reflect v1.4.4–v1.4.8 |
| `README.md` | Version, features, settings table updated |
| `.raiden/state/GOALS.md` | Phase status and shipped list updated |
| `.raiden/state/last-audit.md` | Contains malformed report reference (conversation ID, not file path) — see OBS-2 |
| `.gitignore` | Added `check_xaml.cpp` exclusion |

### State publication paths used

`.raiden/state/` exists; canonical paths used:
- `.raiden/state/AUDIT_LOG.md` (prepended)
- `.raiden/state/last-audit.md` (overwritten)

### Approximate duration

~12 minutes (orientation read, per-category checks, link verification, git history pattern search, report write).
